// import { useNavigate } from 'react-router-dom';

// export default function Redirect(rota: string) {
//   const navigateTo = useNavigate();

//   console.log(
//     `redirecionando para a pagina ${rota.replace('/', '')}...`,
//     navigateTo
//   );

//   return navigateTo(rota);
// }
